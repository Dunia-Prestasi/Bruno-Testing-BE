info:
  name: "[PUT] Replace all permissions granted to a role"
  type: http
  seq: 12
  tags:
    - "31 Admin - Roles and Permissions"

http:
  method: PUT
  url: "{{base_url}}/rbac/roles/:id/permissions"
  params:
    - name: "id"
      value: "{{role_id}}"
      type: path
      description: "Role ID"
  body:
    type: json
    data: |-
      {
        "permission_ids": [
          "{{permission_id}}"
        ]
      }
  auth:
    type: bearer
    token: "{{admin_access_token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("server memberikan respons HTTP", function() {
          expect(res.status).to.be.within(100, 599);
        });

        test("status sesuai kontrak atau middleware aplikasi", function() {
          expect([200, 400, 401, 403, 404, 409, 422, 429, 500, 502, 503]).to.include(res.status);
        });

settings:
  encodeUrl: true
  timeout: 30000
  followRedirects: true
  maxRedirects: 5

docs: |-
  # Replace all permissions granted to a role

  Sets a role's full permission set in a single call. permission_ids is the complete desired set (e.g. everything currently checked in a permission checklist UI), not a delta - permissions missing from the list are revoked and new ones are granted in one transaction.

  - Route: `PUT /rbac/roles/{id}/permissions`
  - Kelompok/role: `31 Admin - Roles and Permissions`
  - Status sukses yang didokumentasikan: `200`
  - Bearer token: environment `{{admin_access_token}}`

  Parameter contoh sudah diisi; parameter query opsional dinonaktifkan agar mudah dipilih saat diperlukan.

  Body dan ID menggunakan data dummy/environment. Sesuaikan variabel ID setelah resource terkait dibuat.
