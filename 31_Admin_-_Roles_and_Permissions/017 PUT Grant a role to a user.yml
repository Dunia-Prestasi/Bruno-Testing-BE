info:
  name: "[PUT] Grant a role to a user"
  type: http
  seq: 17
  tags:
    - "31 Admin - Roles and Permissions"

http:
  method: PUT
  url: "{{base_url}}/rbac/users/:userID/roles/:roleID"
  params:
    - name: "userID"
      value: "{{user_id}}"
      type: path
      description: "User ID"
    - name: "roleID"
      value: "{{role_id}}"
      type: path
      description: "Role ID"
  auth:
    type: bearer
    token: "{{admin_access_token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("server memberikan respons HTTP", function() {
          expect(res.status).to.be.within(100, 599);
        });

        test("status sesuai kontrak atau middleware aplikasi", function() {
          expect([204, 400, 401, 403, 404, 409, 422, 429, 500, 502, 503]).to.include(res.status);
        });

settings:
  encodeUrl: true
  timeout: 30000
  followRedirects: true
  maxRedirects: 5

docs: |-
  # Grant a role to a user

  Request siap pakai untuk pengujian manual API Dunia Prestasi.

  - Route: `PUT /rbac/users/{userID}/roles/{roleID}`
  - Kelompok/role: `31 Admin - Roles and Permissions`
  - Status sukses yang didokumentasikan: `204`
  - Bearer token: environment `{{admin_access_token}}`

  Parameter contoh sudah diisi; parameter query opsional dinonaktifkan agar mudah dipilih saat diperlukan.

  Body dan ID menggunakan data dummy/environment. Sesuaikan variabel ID setelah resource terkait dibuat.
