info:
  name: "[POST] Create a batch for a course"
  type: http
  seq: 8
  tags:
    - 21 Teacher - Courses and Content

http:
  method: POST
  url: "{{base_url}}/courses/:id/batches"
  params:
    - name: id
      value: "{{course_id}}"
      type: path
      description: Course ID
  body:
    type: json
    data: |-
      {
        "batches": [
          { "name": "Batch A - Juli 2026", "branch_id": "{{branch_id}}", "max_students": 30 },
          { "name": "Batch B - Agustus 2026", "branch_id": "{{branch_id}}", "max_students": 25 },
          { "name": "Batch C - September 2026", "branch_id": "{{branch_id}}", "max_students": 40 }
        ]
      }
  auth:
    type: bearer
    token: "{{teacher_access_token}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        if (res.status >= 200 && res.status < 300 && res.body) {
          const data = res.body.data || res.body;
          if (data && data.id) bru.setEnvVar("batch_id", data.id);
        }
    - type: tests
      code: |-
        test("server memberikan respons HTTP", function() {
          expect(res.status).to.be.within(100, 599);
        });

        test("status sesuai kontrak atau middleware aplikasi", function() {
          expect([201, 400, 401, 403, 404, 409, 422, 429, 500, 502, 503]).to.include(res.status);
        });

settings:
  encodeUrl: true
  timeout: 30000
  followRedirects: true
  maxRedirects: 5

docs: |-
  # Create a batch for a course

  Request siap pakai untuk pengujian manual API Dunia Prestasi.

  - Route: `POST /courses/{id}/batches`
  - Kelompok/role: `21 Teacher - Courses and Content`
  - Status sukses yang didokumentasikan: `201`
  - Bearer token: environment `{{teacher_access_token}}`

  Parameter contoh sudah diisi; parameter query opsional dinonaktifkan agar mudah dipilih saat diperlukan.

  Body dan ID menggunakan data dummy/environment. Sesuaikan variabel ID setelah resource terkait dibuat.
