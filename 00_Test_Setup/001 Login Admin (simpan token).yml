info:
  name: Login Admin (simpan token)
  type: http
  seq: 1
  tags:
    - setup

http:
  method: POST
  url: "{{base_url}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "{{admin_email}}",
        "password": "{{admin_password}}"
      }

runtime:
  variables:
    - name: admin_email
      value: superadmin@duniaprestasi.com
    - name: admin_password
      value: superadmin123
    - name: base_url
      value: localhost:8080/api/v1
  scripts:
    - type: after-response
      code: |-
        if (res.status === 200 && res.body && res.body.data) {
          bru.setEnvVar("admin_access_token", res.body.data.access_token || "");
          bru.setEnvVar("admin_refresh_token", res.body.data.refresh_token || "");
          bru.setEnvVar("access_token", res.body.data.access_token || "");
          bru.setEnvVar("refresh_token", res.body.data.refresh_token || "");
          if (res.body.data.user && res.body.data.user.id) {
            bru.setEnvVar("admin_user_id", res.body.data.user.id);
            bru.setEnvVar("user_id", res.body.data.user.id);
          }
        }
    - type: tests
      code: |-
        test("login berhasil", function() {
          expect(res.status).to.equal(200);
          expect(res.body.data.access_token).to.be.a("string");
        });

settings:
  encodeUrl: true
  timeout: 30000
  followRedirects: true
  maxRedirects: 5

docs: Login helper untuk mengisi environment `admin_access_token`, `admin_refresh_token`, dan `admin_user_id` secara otomatis.
