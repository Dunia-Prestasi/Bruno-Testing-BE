info:
  name: "[POST] Create a new course subscription"
  type: http
  seq: 3
  tags:
    - 25 Teacher - Enrollment and Certificates

http:
  method: POST
  url: "{{base_url}}/course-subscriptions"
  body:
    type: json
    data: |-
      {
        "auto_renew": false,
        "cancellation_reason": "Pengujian pembatalan",
        "cancelled_at": "2026-08-01T10:00:00Z",
        "course_id": "{{course_id}}",
        "expired_at": "2027-07-17T00:00:00Z",
        "order_id": "{{order_id}}",
        "plan_id": "{{subscription_plan_id}}",
        "renewed_from_id": "{{course_subscription_id}}",
        "started_at": "2026-07-17T00:00:00Z",
        "status": "active",
        "student_id": "{{student_id}}"
      }
  auth:
    type: bearer
    token: "{{teacher_access_token}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        if (res.status >= 200 && res.status < 300 && res.body) {
          const data = res.body.data || res.body;
          if (data && data.id) bru.setEnvVar("course_subscription_id", data.id);
        }
    - type: tests
      code: |-
        test("server memberikan respons HTTP", function() {
          expect(res.status).to.be.within(100, 599);
        });

        test("status sesuai kontrak atau middleware aplikasi", function() {
          expect([201, 400, 401, 403, 404, 409, 422, 429, 500, 502, 503]).to.include(res.status);
        });

settings:
  encodeUrl: true
  timeout: 30000
  followRedirects: true
  maxRedirects: 5

docs: |-
  # Create a new course subscription

  Request siap pakai untuk pengujian manual API Dunia Prestasi.

  - Route: `POST /course-subscriptions`
  - Kelompok/role: `25 Teacher - Enrollment and Certificates`
  - Status sukses yang didokumentasikan: `201`
  - Bearer token: environment `{{teacher_access_token}}`

  Body dan ID menggunakan data dummy/environment. Sesuaikan variabel ID setelah resource terkait dibuat.
