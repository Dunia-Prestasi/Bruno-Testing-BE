info:
  name: "[PUT] Update an existing course subscription"
  type: http
  seq: 5
  tags:
    - "25 Teacher - Enrollment and Certificates"

http:
  method: PUT
  url: "{{base_url}}/course-subscriptions/:id"
  params:
    - name: "id"
      value: "{{course_subscription_id}}"
      type: path
      description: "Course Subscription ID"
  body:
    type: json
    data: |-
      {
        "auto_renew": false,
        "cancellation_reason": "Pengujian pembatalan",
        "cancelled_at": "contoh cancelled at",
        "course_id": "{{course_id}}",
        "expired_at": "2027-07-17T00:00:00Z",
        "order_id": "{{order_id}}",
        "plan_id": "{{subscription_plan_id}}",
        "renewed_from_id": "{{course_subscription_id}}",
        "started_at": "2026-07-17T00:00:00Z",
        "status": "active",
        "student_id": "{{student_id}}"
      }
  auth:
    type: bearer
    token: "{{teacher_access_token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("server memberikan respons HTTP", function() {
          expect(res.status).to.be.within(100, 599);
        });

        test("status sesuai kontrak atau middleware aplikasi", function() {
          expect([200, 400, 401, 403, 404, 409, 422, 429, 500, 502, 503]).to.include(res.status);
        });

settings:
  encodeUrl: true
  timeout: 30000
  followRedirects: true
  maxRedirects: 5

docs: |-
  # Update an existing course subscription

  Request siap pakai untuk pengujian manual API Dunia Prestasi.

  - Route: `PUT /course-subscriptions/{id}`
  - Kelompok/role: `25 Teacher - Enrollment and Certificates`
  - Status sukses yang didokumentasikan: `200`
  - Bearer token: environment `{{teacher_access_token}}`

  Parameter contoh sudah diisi; parameter query opsional dinonaktifkan agar mudah dipilih saat diperlukan.

  Body dan ID menggunakan data dummy/environment. Sesuaikan variabel ID setelah resource terkait dibuat.
